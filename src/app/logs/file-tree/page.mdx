import { FileTreeDemo } from "@/components/logs/file-tree-demo";
import { BackButton } from "@/components/ui/back-button";

export const metadata = {
  title: "File Tree",
  description: "A recursive file tree component with collapsible folders, file type icons, and interactive schema input.",
};

<BackButton href="/logs" label="back to logs" />

#### FILE TREE

a recursive file tree component built from an interview question. paste an indentation-based schema on the left and see the collapsible folder structure render live on the right.

<FileTreeDemo />
